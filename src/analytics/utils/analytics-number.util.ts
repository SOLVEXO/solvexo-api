/* eslint-disable prettier/prettier */
// Re-exported from the project-wide shared location — analytics and finance both
// round money the same way, so there is exactly one implementation.
export { round } from '../../common/number.util';
